# ASG Builder для Dart

Построитель абстрактного семантического графа (ASG) для языка программирования Dart с автоматическим удалением недостижимого кода (dead code elimination).

## Описание

Проект реализует построение ASG для программ на Dart согласно требованиям задания:
- **Язык**: Dart
- **Модель**: ASG (Abstract Semantic Graph)
- **Парсинг**: Использует только официальный пакет `analyzer` для построения AST
- **Визуализация**: Генерация DOT-файлов и PNG-изображений через Graphviz
- **Dead Code Elimination**: Автоматическое удаление недостижимого кода из графа

## Возможности

### Поддерживаемые конструкции
- ✅ Функции и методы
- ✅ Классы
- ✅ Переменные (var, final, const)
- ✅ Условные операторы (if-else)
- ✅ Циклы (for, while, do-while)
- ✅ Вложенные конструкции
- ✅ Вызовы методов
- ✅ Бинарные операции
- ✅ Литералы (int, String, bool)
- ✅ Return statements

### Анализ достижимости кода
Автоматически удаляет из графа:
- Код после `return` в функциях
- Код после `return` в условных блоках
- Недостижимые операторы в последовательностях

## Структура проекта

```
itmo-dart-asg/
├── src/
│   ├── main.dart                    # Точка входа
│   └── asg/
│       ├── asg_builder.dart         # Построитель ASG
│       ├── dot_visualizer.dart      # Генератор DOT
│       ├── reachability_analyzer.dart # Анализ достижимости
│       └── models/
│           ├── asg_node.dart        # Типы узлов
│           └── asg_graph.dart       # Граф
├── test/
│   └── test_programs/               # Тестовые программы
│       ├── test_sum.dart
│       ├── test_class.dart
│       ├── test_loops.dart
│       ├── test_conditionals.dart
│       ├── test_functions.dart
│       └── test_dead_code.dart
├── output/                          # Генерируемые файлы
│   ├── *.dot                        # DOT файлы
│   └── *.png                        # PNG изображения
├── pubspec.yaml
└── README.md
```

## Установка и запуск

### Требования
- Dart SDK ≥ 3.5.4
- Graphviz (опционально, для генерации PNG)

### Установка зависимостей
```bash
dart pub get
```

### Запуск
```bash
dart run src/main.dart
```

Результаты будут сохранены в директории `output/`:
- `*.dot` - файлы в формате DOT
- `*.png` - изображения графов (если установлен Graphviz)

## Типы узлов ASG

- **FunctionNode** - функции и методы
- **ClassNode** - классы
- **VariableNode** - переменные
- **ParameterNode** - параметры функций
- **BlockNode** - блоки кода
- **IfStatementNode** - условные операторы
- **ForLoopNode** - циклы for
- **WhileLoopNode** - циклы while
- **ReturnNode** - операторы return
- **AssignmentNode** - присваивания
- **MethodCallNode** - вызовы методов
- **BinaryOperationNode** - бинарные операции
- **LiteralNode** - литералы
- **ExpressionNode** - выражения

## Типы рёбер

- **controlFlow** - поток управления
- **dataFlow** - поток данных
- **reference** - ссылки
- **contains** - содержание
- **calls** - вызовы
- **defines** - определения
- **uses** - использование

## Примеры

### Простая функция
```dart
int sum(int a, int b) {
  return a + b;
}
```

### Функция с dead code
```dart
void functionWithDeadCode() {
  int x = 5;
  print(x);
  
  return;  // Всё что после - dead code
  
  int y = 10;  // ❌ Не попадёт в граф
  print(y);    // ❌ Не попадёт в граф
}
```

### Класс с методами
```dart
class Calculator {
  int add(int a, int b) {
    return a + b;
  }
  
  int multiply(int a, int b) {
    return a * b;
  }
}
```

## Визуализация

Граф визуализируется с цветовой кодировкой:
- 🔵 Функции - голубой
- 🟢 Переменные - зелёный
- 🟡 Условия - жёлтый
- 🟠 Циклы - оранжевый
- 🔴 Return - красный
- 🟣 Вызовы методов - фиолетовый

## Соответствие заданию

✅ **Язык**: Dart  
✅ **Модель**: ASG (Abstract Semantic Graph)  
✅ **Парсинг**: Только `analyzer` (AST)  
✅ **Ветвления**: if-else  
✅ **Циклы**: for, while, do-while  
✅ **Вложенность**: поддерживается  
✅ **DOT формат**: генерируется  
✅ **Визуализация**: PNG через Graphviz  
✅ **Тесты**: 6 программ  
✅ **Dead Code Elimination**: реализовано

## Зависимости

```yaml
dependencies:
  analyzer: ^7.7.1  # Парсинг Dart кода
  path: ^1.9.1      # Работа с путями
  collection: ^1.19.1  # Коллекции
```

## Автор

Проект выполнен в рамках задания по построению моделей программ.